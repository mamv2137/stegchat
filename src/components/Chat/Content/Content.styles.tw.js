import styled from "styled-components";
import tw from "twin.macro";

export const ContentContainer = styled.div`
  ${tw`w-full flex flex-col flex-1 bg-transparent p-4 overflow-y-auto h-full`}
`;
